Why use the YouTube API instead of hardcoding video IDs?

Steve uploads videos almost daily.

The playlist is the source of truth.

The app automatically receives new videos without requiring a software update.